# План обучения ML-модели для BabylonFish

## 📊 Текущее состояние датасета

### Статистика
- **Всего примеров:** 1031
- **Распределение:**
  - `en` (английский): 383 примеров (37.1%)
  - `ru` (русский): 328 примеров (31.8%)
  - `ru_wrong` (ошибочная раскладка): 320 примеров (31.1%)

### Балансировка
Датасет сбалансирован, что хорошо для обучения модели без смещения (bias).

## 🎯 Цели обучения

### Основная задача
Классифицировать текст на три категории:
1. **en** — правильный английский текст
2. **ru** — правильный русский текст
3. **ru_wrong** — русский текст, напечатанный на английской раскладке

### Ключевые вызовы
- Модель должна отличать `ru_wrong` от `en` (обе могут выглядеть как бессмысленные символы)
- Модель должна работать с короткими словами (2-3 символа)
- Модель должна обрабатывать фразы и предложения
- Модель должна быть быстрой (real-time детекция при печати)

## 🛠 Технологии

### Обучающий скрипт
- **Файл:** `ML/train_model.swift`
- **Фреймворк:** Apple CreateML (MLTextClassifier)
- **Тип модели:** Maximum Entropy Classifier для текста

### Разделение данных
- **Training set:** 80% (~825 примеров)
- **Test set:** 20% (~206 примеров)
- **Random seed:** 5 (для воспроизводимости)

## 📝 План обучения

### Шаг 1: Подготовка окружения ⏱ 5 мин

#### Требования
- macOS (CreateML доступен только на macOS)
- Xcode Command Line Tools
- Swift 5.0+

#### Проверка
```bash
xcode-select --install  # если не установлен
swift --version
```

### Шаг 2: Запуск обучения ⏱ 2-5 мин

```bash
cd /tmp/babylonfish
swift ML/train_model.swift
```

**Ожидаемый результат:**
- Метрика accuracy (точность) на тестовом наборе
- Сохраненная модель: `ML/Models/BabylonFishClassifier.mlmodel`

### Шаг 3: Оценка качества ⏱ 5 мин

#### Метрики для проверки
После обучения модель покажет:
- **Training Accuracy** — точность на тренировочном наборе
- **Classification Error** — ошибка классификации

#### Целевые значения
- Accuracy ≥ 90% — хорошо
- Accuracy ≥ 95% — отлично
- Accuracy < 85% — нужно улучшать датасет

### Шаг 4: Тестирование модели ⏱ 10-15 мин

#### Создание тестового скрипта
```swift
import CreateML

let modelURL = URL(fileURLWithPath: "ML/Models/BabylonFishClassifier.mlmodel")
let model = try MLTextClassifier(contentsOf: modelURL)

// Тестовые примеры
let testCases = [
    ("hello", "en"),
    ("привет", "ru"),
    ("ghbdtn", "ru_wrong"),
    ("rfr ltkf", "ru_wrong"),
    ("programming", "en"),
    ("программирование", "ru"),
    ("ghbdtn rfr ltkf", "ru_wrong"),
    ("how are you", "en"),
    ("как дела", "ru")
]

for (text, expectedLabel) in testCases {
    if let prediction = try? model.prediction(for: text) {
        let predictedLabel = prediction.label
        let isCorrect = predictedLabel == expectedLabel
        print("\(text): \(predictedLabel) [\(isCorrect ? "✓" : "✗")]")
    }
}
```

### Шаг 5: Интеграция в BabylonFish ⏱ 30-60 мин

#### Где использовать модель
В `Sources/BabylonFish2/Language/LanguageDetector.swift`:

1. **Загрузка модели** при инициализации:
```swift
import CreateML

class LanguageDetector {
    private var classifier: MLTextClassifier?

    init(learningManager: LearningManager) {
        self.learningManager = learningManager

        // Загружаем ML модель если она существует
        if let modelURL = Bundle.main.url(forResource: "BabylonFishClassifier", withExtension: "mlmodel") {
            do {
                self.classifier = try MLTextClassifier(contentsOf: modelURL)
                logDebug("ML Classifier loaded successfully")
            } catch {
                logDebug("Failed to load ML Classifier: \(error)")
            }
        }
    }
}
```

2. **Использование модели** в `detectLanguage`:
```swift
func detectLanguage(for keyCodes: [Int]) -> Language? {
    guard keyCodes.count >= 1 else { return nil }

    // Сначала пробуем ML модель
    if let classifier = self.classifier {
        // Получаем строку из keyCodes
        if let enString = getEnString(from: keyCodes) {
            do {
                let prediction = try classifier.prediction(for: enString.lowercased())
                let label = prediction.label

                // Преобразуем метку в Language
                switch label {
                case "en":
                    return .english
                case "ru":
                    return .russian
                case "ru_wrong":
                    return .russian  // ru_wrong означает, что текст должен быть русским
                default:
                    break
                }
            } catch {
                logDebug("ML prediction failed: \(error)")
                // Fall back to original logic
            }
        }
    }

    // Fall back к оригинальной логике детекции
    // ... оригинальный код ...
}
```

### Шаг 6: Сборка приложения ⏱ 5-10 мин

```bash
cd /tmp/babylonfish
./install_app.sh
```

### Шаг 7: Тестирование в реальном использовании ⏱ 30-60 мин

#### Сценарии для тестирования
1. **Английский текст:**
   - Печатать "hello world" — должен оставаться английским
   - Печатать "programming is fun" — должен оставаться английским

2. **Русский текст:**
   - Печатать "привет мир" — должен оставаться русским
   - Печатать "как дела" — должен оставаться русским

3. **Ошибочная раскладка:**
   - Печатать "ghbdtn" (на английской раскладке) — должен переключиться на русский
   - Печатать "rfr ltkf" — должен переключиться на русский

4. **Краевые случаи:**
   - Короткие слова (2-3 символа)
   - Слова с цифрами
   - Слова с символами

## 🔧 Улучшение модели (если accuracy < 90%)

### Вариант 1: Расширить датасет
Добавить больше примеров в `sample_dataset.csv`:
- Больше сленга (для en)
- Больше разговорных фраз
- Больше технических терминов
- Больше имен собственных

### Вариант 2: Добавить шум в датасет
Создать варианты с опечатками:
- "hllo" вместо "hello"
- "privet" вместо "привет"
- "proramming" вместо "programming"

### Вариант 3: Использовать другой классификатор
CreateML поддерживает разные модели:
- `MLTabularClassifier` — для табличных данных
- `MLBoostedTreeClassifier` — градиентный бустинг
- `MLRandomForestClassifier` — случайный лес

## 📋 Чеклист завершения

- [ ] Датасет содержит минимум 1000 примеров
- [ ] Датасет сбалансирован (примерно равное количество en/ru/ru_wrong)
- [ ] Модель обучена (accuracy ≥ 90%)
- [ ] Модель сохранена в `ML/Models/BabylonFishClassifier.mlmodel`
- [ ] Модель интегрирована в `LanguageDetector.swift`
- [ ] Приложение пересобрано
- [ ] Ручное тестирование пройдено успешно
- [ ] Автоматические тесты (если есть) проходят

## 🚀 Дополнительные возможности

### Динамическое обучение
Добавить возможность "дообучать" модель на основе пользовательских предпочтений:
- Сохранять решения пользователя
- Периодически переобучать модель с новыми данными
- Использовать Transfer Learning

### А/Б тестирование
Запустить обе реализации (старую эвристику и ML) параллельно:
- Сравнивать точность
- Собирать feedback от пользователей
- Выбрать лучшую

### Логирование
Добавить детальное логирование предсказаний:
- Записывать текст и предсказание
- Записывать фактический выбор пользователя
- Использовать для анализа ошибок

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи: `~/Library/Logs/BabylonFish/`
2. Проверьте права доступа: Системные настройки → Конфиденциальность и безопасность
3. Проверьте что модель загружается (ищите "ML Classifier loaded" в логах)

---

**Время на реализацию:** ~2-3 часа для первого цикла
**Сложность:** Средняя (требуется familiarity с Swift и CreateML)
**Риск:** Низкий (есть fallback к оригинальной логике)
