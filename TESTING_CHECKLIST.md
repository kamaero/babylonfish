# BabylonFish Enhanced v3.0.60+ Testing Checklist

## Установка и настройка

### ✅ Шаг 1: Установка приложения
1. Скрипт установки уже выполнен: `./install_enhanced_typo.sh`
2. Приложение установлено в: `/Users/xkr/Applications/BabylonFish Enhanced.app`
3. Bundle ID: `com.babylonfish.app.enhanced`

### ✅ Шаг 2: Настройка разрешений
1. Откройте **System Settings → Privacy & Security**
2. Перейдите в **Accessibility**:
   - Нажмите "+" (Add)
   - Выберите `BabylonFish Enhanced.app` из `/Users/xkr/Applications/`
3. Перейдите в **Input Monitoring**:
   - Нажмите "+" (Add)
   - Выберите `BabylonFish Enhanced.app` из `/Users/xkr/Applications/`

### ✅ Шаг 3: Запуск приложения
1. Откройте `/Users/xkr/Applications/BabylonFish Enhanced.app`
2. Проверьте, что иконка появилась в меню-баре (правый верхний угол)
3. Откройте лог для мониторинга: `tail -f ~/babylonfish_debug.log`

## Тестирование основных функций

### 🔄 Тест 1: Исправление опечаток (CoreML+NSSpellChecker)
**Цель:** Проверить интеграцию CoreML с системным словарем

**Сценарии:**
1. **Английские опечатки:**
   - Введите: `teh` → должно стать: `the`
   - Введите: `recieve` → должно стать: `receive`
   - Введите: `seperate` → должно стать: `separate`

2. **Русские опечатки:**
   - Введите: `првиет` → должно стать: `привет`
   - Введите: `карова` → должно стать: `корова`
   - Введите: `машина` → должно стать: `машина` (без изменений)

3. **Смешанные опечатки:**
   - Введите: `teh привет` → должно стать: `the привет`
   - Введите: `hello првиет` → должно стать: `hello привет`

**Что проверять в логах:**
```
[EnhancedTypoCorrector] Processing word: 'teh'
[EnhancedTypoCorrector] CoreML confidence: 0.85 (English)
[EnhancedTypoCorrector] NSSpellChecker suggestions: ['the', 'tech', 'ten']
[EnhancedTypoCorrector] Selected correction: 'the' (score: 0.92)
```

### 🔄 Тест 2: Короткие слова через NSSpellChecker
**Цель:** Проверить обработку коротких слов (2-3 символа)

**Сценарии:**
1. **Русские короткие слова в английской раскладке:**
   - Введите: `rfr` → должно стать: `как`
   - Введите: `cnjq` → должно стать: `слово`
   - Введите: `фку` → должно стать: `are` (проверить!)

2. **Английские короткие слова в русской раскладке:**
   - Введите: `руддщ` → должно стать: `hello`
   - Введите: `рщц` → должно стать: `how`
   - Введите: `яку` → должно стать: `you` (проверить!)

**Что проверять:**
- Работает ли NSSpellChecker для коротких слов
- Правильно ли определяется язык
- Происходит ли переключение раскладки

### 🔄 Тест 3: Обратная конверсия (фку, яку)
**Цель:** Исправить оставшиеся проблемы с обратной конверсией

**Проблемные слова:**
1. `фку` → должно стать: `are`
   - Текущий статус: должно работать через NSSpellChecker
   - Проверить: работает ли автоматически

2. `яку` → должно стать: `you`
   - Текущий статус: требует добавления в паттерны
   - Проверить: работает ли через NSSpellChecker

**Действия при неудаче:**
1. Если `фку` не работает:
   - Добавить в `englishInRussianPatterns` в `EventProcessor.swift`
   - Пересобрать приложение

2. Если `яку` не работает:
   - Добавить в `englishInRussianPatterns` в `EventProcessor.swift`
   - Пересобрать приложение

### 🔄 Тест 4: Контекстная обработка
**Цель:** Проверить работу контекстного анализа

**Сценарии:**
1. **Игнорирование парольных полей:**
   - Откройте любой сайт с полем пароля
   - Введите `ghbdtn` в поле пароля
   - Проверить: НЕ должно меняться на `привет`

2. **Многословные предложения:**
   - Введите: `ghbdtn rfr ltkf` → должно стать: `привет как дела`
   - Проверить последовательную обработку

3. **Пунктуация:**
   - Введите: `ghbdtn!` → должно стать: `привет!`
   - Введите: `"руддщ"` → должно стать: `"hello"`

### 🔄 Тест 5: Производительность и стабильность
**Цель:** Проверить отсутствие лагов и сбоев

**Что проверять:**
1. **Скорость реакции:**
   - Вводите текст быстро
   - Проверьте, нет ли задержек
   - В логах: `Processing time: X ms`

2. **Защита от рекурсии:**
   - Проверьте лог на наличие предупреждений:
   ```
   [EventTapManager] Recursion protection: ignoring event from BabylonFish
   ```

3. **Потребление памяти:**
   - Откройте Activity Monitor
   - Проверьте память BabylonFish (должно быть < 100MB)

## Процесс тестирования

### 📋 Чеклист для каждого теста:
- [ ] Открыть текстовый редактор (TextEdit, Notes, VS Code)
- [ ] Включить мониторинг логов: `tail -f ~/babylonfish_debug.log`
- [ ] Выполнить тестовый сценарий
- [ ] Зафиксировать результат
- [ ] Проверить логи на наличие ошибок
- [ ] Записать наблюдения

### 📝 Формат записи результатов:
```
Тест: [Название теста]
Дата: [Дата тестирования]
Результат: ✅ Успешно / ❌ Неудачно / ⚠️ Частично
Наблюдения:
- [Конкретное наблюдение 1]
- [Конкретное наблюдение 2]
Логи:
[Ключевые строки из лога]
```

### 🔧 Действия при обнаружении проблем:

#### Проблема: Не работает исправление опечаток
**Решение:**
1. Проверить лог на наличие `[EnhancedTypoCorrector]`
2. Проверить, загружена ли CoreML модель
3. Проверить работу NSSpellChecker через тестовый скрипт

#### Проблема: Не переключаются короткие слова
**Решение:**
1. Проверить `minWordLengthForSwitch` в `EventProcessor.swift`
2. Проверить паттерны в `englishInRussianPatterns`
3. Проверить логи детекции языка

#### Проблема: Рекурсия или зависание
**Решение:**
1. Проверить `isSendingEvents` в `EventTapManager.swift`
2. Проверить максимальную глубину рекурсии
3. Перезапустить приложение

## Быстрые команды для тестирования

### Мониторинг логов:
```bash
# Просмотр логов в реальном времени
tail -f ~/babylonfish_debug.log

# Очистка логов
> ~/babylonfish_debug.log

# Поиск ошибок
grep -i "error\|fail\|warning" ~/babylonfish_debug.log
```

### Управление приложением:
```bash
# Остановить BabylonFish
pkill -9 "BabylonFish"

# Запустить BabylonFish
open /Users/xkr/Applications/BabylonFish\ Enhanced.app

# Проверить процессы
ps aux | grep BabylonFish
```

### Сборка и переустановка:
```bash
# Пересобрать приложение
./install_enhanced_typo.sh

# Сбросить разрешения
tccutil reset Accessibility com.babylonfish.app.enhanced
tccutil reset InputMonitoring com.babylonfish.app.enhanced
```

## Следующие шаги после тестирования

### При успешном тестировании:
1. ✅ Обновить `AGENTS.md` с результатами
2. ✅ Создать релизную версию
3. ✅ Протестировать на других машинах

### При обнаружении проблем:
1. 🔧 Исправить `фку`/`яку` конверсию
2. 🔧 Оптимизировать производительность
3. 🔧 Добавить больше тестовых сценариев

### Долгосрочные задачи:
1. 🚀 Реализация автодополнения (Т9)
2. 🚀 Визуальный режим обучения CoreML
3. 🚀 UI индикаторы переключения раскладки

---

**Удачи в тестировании!** 🎯

Если возникнут вопросы или проблемы, проверьте:
1. Логи: `~/babylonfish_debug.log`
2. Разрешения в System Settings
3. Версию приложения в меню-баре